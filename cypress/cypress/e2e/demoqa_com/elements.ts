

describe('elements', () => {
    beforeEach(() => {
       cy.visit('/elements');
       // cy.url().should('contain', 'elements');
       // cy.get('.header-text').contains('Elements').should('be.visible');
    });

    it('go to login page ', () => {
        cy.log('dsda');
    });

});

